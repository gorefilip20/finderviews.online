/**
 * Market coverage now lives in shared/ so the server can enforce the same eligibility
 * rules the UI displays. Re-exported here to keep existing imports working.
 */
export * from "@shared/marketCoverage";
